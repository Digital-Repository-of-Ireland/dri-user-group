<%= render :partial => "user_group/shared/header" %>

<h1>Static#home</h1>
<% if user_signed_in? %>
    
    <p> <%= current_user.email %> is logged in</p>
<% else %>
    <%= link_to (t :'user_groups.views.shared.sign_in'), new_user_session_path %> 
    <%= link_to (t :'user_groups.views.shared.register'), new_user_path %>
    
<% end %>

<%= render 'debug_routes' %>

