<%= paginate @memberships %>

<% for membership in @memberships %>
    <% if object.is_a?(UserGroup::User) %>
        <% group = UserGroup::Group.find_by_id(membership.group_id) %>
        <%= membership.updated_at %>
        <%= link_to (t :'user_groups.views.partials.leave_group.leave') + group.name.capitalize unless group.nil? %>
    <% elsif object.is_a?(UserGroup::Group) %>
        <% user = UserGroup::User.find_by_id(membership.user_id) %>
        <%= user.full_name + " (" + user.email + ")" unless user.nil? %>
        <%= membership.updated_at %>

        <%= link_to (t :'user_groups.views.partials.approved.remove'), 
          remove_read_membership_path(membership), method: :delete %>
    <% end %>
    
    <br/>
<% end %>


