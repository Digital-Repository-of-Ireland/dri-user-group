<h1> All users (<%= User.count %>)</h1> 

<ul class="users">
<% User.all.each do |user| %>
    <%= link_to user.email, user %> | <%= link_to "delete", user, method: :delete, confirm: "Are you sure?" %><br />
<% end %>

</ul>