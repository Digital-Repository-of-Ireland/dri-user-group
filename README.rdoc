= UserGroup =

Install Gem:
1) gem install user_group-<version>.gem

Use In Rails Application:
To install this engine you must first go to the rails application in which this will be included. Perform the following actions:
1) Edit your gemfile to include user_group (gem 'user_group')
    Note: user_group includes devise by default.
2) bundle update
3) rake user_group:install:migrations
4) rake db:migrate
5) edit your applications db/seeds.rb file to include UserGroup::Engine.load_seed
6) rake db:seed
7) Edit the routes file (config/routes.rb) to include the line
    mount UserGroup::Engine => "user_groups"
    where "user_groups" is the path you want to mount the engine at.
8) Start the server and go to localhost:3000/user_groups and login as email: admin@example.com, password: password
9) Ensure your application provides a main_app route

To enable the forgot password link you must set up mailer.
  add config.action_mailer.default_url_options = { :host => 'localhost:3000' }
  to config/environments/development.rb

  This will show the link the user must go to in the console.

Changes:
v0.3
  Major:
    Created config file as initializer
    Fixed user not being able to logout in test env
    Added devise guests
    Updated error messages to use a flash notice
    Added cookie confirm from rnag app
    Autoload user modules rather than require
    Updated devise views (from rnag app) and localisation strings
    Removed rake bootstrap file & replaced with seeds file
    Removed root_path now requires that the main_app provides a root_path
    Accounts now join the group registered automaticially
    Created UserOptions module for providing info such as language, profile settings etc
    Added header as a partial to views
    Fix for missing routes error
    Moved user model code into a UserSecurity module located at lib/user_group/user_security.rb
    Changed user model to include the UserSecurity
  
    These changes are done to faciilite using this user model with blacklight.
    To use with blacklight first the app/models/user_group/user.rb model should be copied to the same directory in your rails application. Next the line
      include Blacklight::User
    must be included.

    Alternative was to have this gem depend on blacklight (which would help for styling)
    and have the class include the blacklight user automatically.

  Minor:
    Added pagination to lists (through kaminari), useful for users and groups.
    Fixed bug with not being able to edit a group
    Fixed bug with is_locked group method
    Can update users that have no name
    Updated views to have partial structure and seperate admin and ordinary views
    Styling updates
    Groups now show name even when locked




v0.2
  UserGroup now an engine (gem)
  Sample data created
  Static content has been Internationalisied

v0.1
  Initial Prototype
  Basic Functionality

Routes:

(A) - Admin Only
(P) - Public
(L) - Logged In
(V) - Admin Or In Your Account

--Static Pages--
                root        /                              (P)  static#home
                help        /help(.:format)                (P)  static#help
                home        /home(.:format)                (P)  static#home
               admin        /admin(.:format)               (A)  static#admin

 --Sessions--              
    new_user_session GET    /users/sign_in(.:format)       (P)  devise/sessions#new
        user_session POST   /users/sign_in(.:format)       (P)  devise/sessions#create
destroy_user_session DELETE /users/sign_out(.:format)      (L)  devise/sessions#destroy

--Passwords-- (Untested)
       user_password POST   /users/password(.:format)      (?)  devise/passwords#create
   new_user_password GET    /users/password/new(.:format)  (?)  devise/passwords#new
  edit_user_password GET    /users/password/edit(.:format) (?)  devise/passwords#edit
                     PUT    /users/password(.:format)      (?)  devise/passwords#update

--User Accounts--
               users GET    /users(.:format)               (A)  users#index
                     POST   /users(.:format)               (P)  users#create
            new_user GET    /users/new(.:format)           (P)  users#new
           edit_user GET    /users/:id/edit(.:format)      (V)  users#edit
                user GET    /users/:id(.:format)           (V)  users#show
                     PUT    /users/:id(.:format)           (V)  users#update
                     DELETE /users/:id(.:format)           (V)  users#destroy
--Groups--
              groups GET    /groups(.:format)              (L)  groups#index
                     POST   /groups(.:format)              (A)  groups#create
           new_group GET    /groups/new(.:format)          (A)  groups#new
          edit_group GET    /groups/:id/edit(.:format)     (A)  groups#edit
               group GET    /groups/:id(.:format)          (A)  groups#show
                     PUT    /groups/:id(.:format)          (A)  groups#update
                     DELETE /groups/:id(.:format)          (A)  groups#destroy
--Memberships--
 approve_membership PUT    /memberships/:id/approve(.:format)(A)  memberships#approve
 pending_memberships POST   /memberships/pending(.:format)   (L)  memberships#pending
         memberships POST   /memberships(.:format)           (A)  memberships#create
          membership DELETE /memberships/:id(.:format)       (V)  memberships#destroy
